package com.ivo.ganev.awords

import androidx.test.platform.app.InstrumentationRegistry
import org.junit.Test

class AssetJsonLoaderTest {

    @Test
    fun adjectives() {
        val context = InstrumentationRegistry.getInstrumentation().targetContext
    }
}